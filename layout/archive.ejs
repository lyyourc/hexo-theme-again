<main class="archive">
  <% page.posts.each(function(post){ %>
    <%- partial('./_partial/archive', {post: post}) %>
  <% }) %>

  <nav class="page-nav">
    <%- paginator({
      prev_text: '&larr; Prev',
      next_text: 'Next &rarr;'
    }) %>
  </nav>
</main>
